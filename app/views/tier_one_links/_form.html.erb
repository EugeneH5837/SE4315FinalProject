<%= form_with(model: tier_one_link, local: true) do |form| %>
  <% if tier_one_link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tier_one_link.errors.count, "error") %> prohibited this tier_one_link from being saved:</h2>

      <ul>
      <% tier_one_link.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :links %>
    <%= form.text_field :links, id: :tier_one_link_links %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
