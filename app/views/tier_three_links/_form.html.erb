<%= form_with(model: tier_three_link, local: true) do |form| %>
  <% if tier_three_link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tier_three_link.errors.count, "error") %> prohibited this tier_three_link from being saved:</h2>

      <ul>
      <% tier_three_link.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :links %>
    <%= form.text_field :links, id: :tier_three_link_links %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
